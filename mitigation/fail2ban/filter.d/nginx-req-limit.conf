# Fail2Ban filter for Nginx access log flooding
# Matches any successful or rate-limited request from an IP

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD|PUT|DELETE) .* HTTP/.*" [0-9]+ .*$
ignoreregex =
