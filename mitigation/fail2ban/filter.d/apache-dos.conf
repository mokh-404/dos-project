# Fail2Ban filter for Apache DoS detection
# Matches requests in Apache combined/common log format

[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD|PUT|DELETE) .* HTTP/.*" [0-9]+ .*$
ignoreregex =
